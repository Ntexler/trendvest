# TrendVest — שדרוג הפיד הראשי

## מה השתנה

### עמוד ראשי חדש — TrendFeed
העמוד הראשי השתנה מגריד סטטי של כרטיסי נושאים (Dashboard) לפיד חדשות חי בסגנון Twitter/Reddit.

**לפני:** רשת 3 עמודות עם כרטיסי נושאים שכולם נראים אותו דבר. צריך ללחוץ כדי לראות מניות. טרנדים וחדשות בטאבים נפרדים.

**אחרי:** פיד עמודה בודדת. כל פריט = נושא + כתבה מובילה + מניות + ספארקליין אמיתי. חדשות ישראליות ופודקאסטים בצד.

---

## קבצים חדשים

### Backend
| קובץ | מה הוא עושה |
|---|---|
| `backend/app/routers/feed.py` | API חדש — `GET /api/feed` שמשלב מגמות + חדשות + מניות + חדשות ישראליות + פודקאסטים לתשובה אחת |
| `backend/app/services/israeli_news.py` | שירות חדשות ישראליות — מושך RSS מגלובס, כלכליסט, גיקטיים, דה מרקר |
| `backend/app/services/podcasts.py` | שירות פודקאסטים — מגלה פרקים מפודקאסטים ישראליים, עם אפשרות לתמלול (Whisper) וניתוח AI |

### Frontend
| קובץ | מה הוא עושה |
|---|---|
| `frontend/src/components/TrendFeed.tsx` | קומפוננט הפיד החדש — מחליף את Dashboard כעמוד הראשי |

---

## קבצים שהשתנו

### Backend
| קובץ | מה השתנה |
|---|---|
| `backend/app/main.py` | נרשם router חדש — `feed.router` |
| `backend/app/routers/trends.py` | נוסף endpoint — `GET /api/trends/{slug}/history` שמחזיר היסטוריית אזכורים אמיתית (מחליף ספארקליינים מזויפים) |

### Frontend
| קובץ | מה השתנה |
|---|---|
| `frontend/src/app/page.tsx` | מייבא TrendFeed במקום Dashboard. הוסר טאב "חדשות" נפרד (חדשות עכשיו בתוך הפיד) |
| `frontend/src/components/MobileMenu.tsx` | הוסר כפתור "חדשות" מתפריט מובייל (4 כפתורים במקום 5) |
| `frontend/src/lib/types.ts` | נוספו טיפוסים: `FeedItem`, `FeedTopicInfo`, `FeedStock`, `MomentumHistoryPoint`, `PodcastEpisode`, `UnifiedFeed` |
| `frontend/src/lib/api.ts` | נוסף `getFeed()` — קריאה ל-API הפיד המאוחד |
| `frontend/src/i18n/translations.ts` | שם הטאב השתנה מ-"טרנדים" ל-"פיד". נוספו תרגומים לפיד |

---

## API Endpoints חדשים

### `GET /api/feed`
הAPI המרכזי החדש. מחזיר את כל מה שצריך לעמוד הראשי בקריאה אחת:
- רשימת מגמות ממויינת לפי momentum
- היסטוריית אזכורים (לספארקליינים אמיתיים)
- מניות עם מחירים חיים
- כתבה מובילה לכל נושא
- חדשות ישראליות (מגלובס, כלכליסט, גיקטיים, דה מרקר)
- פרקי פודקאסטים אחרונים

### `GET /api/feed/il-news`
חדשות ישראליות בלבד. אפשר לסנן לפי מקור (globes, calcalist, geektime, themarker).

### `GET /api/feed/podcasts`
פרקי פודקאסטים. אפשר לסנן לפי פודקאסט או קטגוריה (finance, tech).

### `POST /api/feed/podcasts/transcribe`
תמלול פרק פודקאסט + ניתוח AI. דורש OPENAI_API_KEY.

### `GET /api/trends/{slug}/history`
היסטוריית momentum אמיתית — מחליף את הספארקליינים המזויפים שנוצרו מ-hash.

---

## מקורות חדשות ישראליים
- **גלובס** — שוק ההון + טכנולוגיה (RSS)
- **כלכליסט** — שוק ההון + טכנולוגיה (RSS)
- **גיקטיים** — טכנולוגיה וסטארטאפים (RSS)
- **דה מרקר** — שוק ההון (RSS)

## פודקאסטים ישראליים
- **המסלול** — פודקאסט השקעות ופיננסים
- **עושים חשבון** — פודקאסט כלכלי של כלכליסט
- **גיקטיים פודקאסט** — טכנולוגיה וסטארטאפים
- **הגרוש שלי** — פודקאסט על כסף וכלכלה

אפשרות לתמלול (Whisper API) וניתוח (Anthropic API) לחילוץ מידע פיננסי מפרקים.

---

## מה עוד אפשר לעשות בהמשך
1. **Auto-refresh** — רענון אוטומטי של הפיד כל כמה דקות
2. **Infinite scroll** — טעינת עוד פריטים בגלילה
3. **פילטר לפי מקור** — סינון לפי חדשות ישראליות / בינלאומיות / פודקאסטים
4. **מועדפים** — שמירת נושאים ספציפיים למעקב
5. **Push notifications** — התראות על שינויי momentum גדולים
